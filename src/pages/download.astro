---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Download RegParity | Request Access">
  <div class="wrap page-content">
    <div class="form-container">
      <h1>Request RegParity</h1>
      <p>Email us from your work address and we’ll send an expiring download link and a short getting-started guide.</p>

      <a href="mailto:hello@regparity.com?subject=RegParity%20download%20request" class="btn primary full-width">Email hello@regparity.com</a>

      <div class="secondary-info">
        <p>Please include your name, company and preferred contact email.</p>
        <p>If your IT team needs to review the application first, feel free to copy them in.</p>
      </div>

      <p class="plain-link">
        <a href="mailto:hello@regparity.com">hello@regparity.com</a>
      </p>
    </div>
  </div>
</BaseLayout>

<style>
  .page-content {
    padding: 80px 22px;
    display: flex;
    justify-content: center;
  }
  .form-container {
    width: 100%;
    max-width: 520px;
  }
  h1 {
    font-size: 2.5rem;
    margin-bottom: 16px;
    color: var(--rpText);
  }
  p {
    color: var(--rpTextMuted);
    margin-bottom: 32px;
    line-height: 1.6;
  }
  .full-width {
    width: 100%;
    margin-bottom: 32px;
    text-align: center;
  }

  .secondary-info p {
    font-size: 0.9rem;
    margin-bottom: 8px;
    color: var(--rpTextMuted);
    opacity: 0.8;
  }

  .plain-link {
    margin-top: 24px;
    font-size: 0.9rem;
    color: var(--rpTextMuted);
    text-align: center;
  }
  .plain-link a {
    color: var(--rpTextMuted);
    text-decoration: underline;
  }
  .plain-link a:hover {
    color: var(--rpText);
  }
</style>
