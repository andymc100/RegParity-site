---
import { Image } from 'astro:assets';
import heroImage from '../assets/screenshots/hero-break-resolution.webp';
---
<section class="hero wrap">
  <div class="hero-content">
    <h1>Fix EMIR and SFTR reconciliation breaks faster.</h1>
    <p>RegParity turns reconciliation and valuation breaks into prioritised corrective actions — with a complete audit trail and compliant outbound updates.</p>

    <div class="trust-strip">
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
          <line x1="9" y1="3" x2="9" y2="21"></line>
        </svg>
        <span>Windows Desktop</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        <span>Offline-First</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
          <polyline points="14 2 14 8 20 8"></polyline>
          <line x1="16" y1="13" x2="8" y2="13"></line>
          <line x1="16" y1="17" x2="8" y2="17"></line>
          <polyline points="10 9 9 9 8 9"></polyline>
        </svg>
        <span>Full Audit Trail</span>
      </div>
    </div>

    <div class="hero-actions">
      <a href="/download" class="btn primary">Request download link</a>
      <a href="/contact" class="btn">Contact</a>
    </div>
  </div>

  <div class="hero-image-container">
    <div class="screenshot">
      <Image src={heroImage} alt="RegParity Break Resolution Dashboard" width={800} density={2} />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 56px 0 30px;
    display: grid;
    grid-template-columns: 1.15fr .85fr;
    gap: 40px;
    align-items: center;
  }

  .hero h1 {
    margin: 0 0 16px;
    font-size: clamp(34px, 4.2vw, 54px);
    line-height: 1.05;
    color: var(--rpText);
  }

  .hero p {
    margin: 12px 0 24px;
    color: var(--rpTextMuted);
    max-width: 62ch;
    font-size: 1.08rem;
  }

  /* Trust Strip */
  .trust-strip {
    display: flex;
    gap: 24px;
    padding-top: 24px;
    margin-bottom: 32px;
    border-top: 1px solid var(--rpBorder);
    color: var(--rpTextMuted);
    font-size: 0.85rem;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    margin-bottom: 32px;
  }

  /* Image Container */
  .hero-image-container {
    max-height: 520px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .screenshot {
    border-radius: 18px;
    overflow: hidden;
    border: 1px solid var(--rpBorder);
    background: white;
    box-shadow: 0 8px 30px rgba(0,0,0,0.06);
    line-height: 0; /* Remove extra space below image */
  }

  /* Ensure aspect ratio and max height */
  .screenshot img {
    width: 100%;
    height: auto;
    aspect-ratio: 16/10;
    object-fit: cover;
    display: block;
  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .hero p {
      margin-left: auto;
      margin-right: auto;
    }
    .trust-strip {
      justify-content: center;
    }
    .hero-actions {
      justify-content: center;
    }
    .hero-image-container {
      margin-top: 24px;
    }
  }
</style>
