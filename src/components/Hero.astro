---
import { Image } from 'astro:assets';
import heroImage from '../assets/screenshots/hero-break-resolution.webp';
---
<section class="hero wrap">
  <div class="hero-content">
    <h1>Fix EMIR and SFTR reconciliation breaks faster.</h1>
    <p>RegParity turns reconciliation and valuation breaks into prioritised corrective actions — with a complete audit trail and compliant outbound updates.</p>

    <div class="trust-strip">
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
          <line x1="9" y1="3" x2="9" y2="21"></line>
        </svg>
        <span>Windows Desktop</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        <span>Offline-First</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
          <polyline points="14 2 14 8 20 8"></polyline>
          <line x1="16" y1="13" x2="8" y2="13"></line>
          <line x1="16" y1="17" x2="8" y2="17"></line>
          <polyline points="10 9 9 9 8 9"></polyline>
        </svg>
        <span>Full Audit Trail</span>
      </div>
    </div>

    <div class="hero-actions">
      <a href="/download" class="btn primary">Request download link</a>
      <a href="/contact" class="btn">Contact</a>
    </div>
  </div>

  <div class="screenshot hero-image-container">
    <Image src={heroImage} alt="RegParity Break Resolution Dashboard" width={800} height={500} />
  </div>
</section>

<style>
  /* Layout Adjustments */
  .hero-actions {
    display: flex;
    gap: 16px;
    margin-bottom: 32px;
  }

  /* Trust Strip */
  .trust-strip {
    display: flex;
    gap: 24px;
    padding-top: 24px;
    margin-bottom: 32px;
    border-top: 1px solid var(--rpBorder);
    color: var(--rpTextMuted);
    font-size: 0.85rem;
    font-weight: 500;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  /* Hero Image Container */
  .hero-image-container {
    aspect-ratio: 16/10;
    max-height: 520px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #FAFBFC; /* Subtle placeholder bg if image loads slowly */
  }

  .hero-image-container img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }

  @media (max-width: 900px) {
    .trust-strip { justify-content: center; }
    .hero-actions { justify-content: center; }
  }
</style>
