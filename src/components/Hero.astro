---
---
<section class="hero wrap">
  <div class="hero-content">
    <div class="kicker">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
      </svg>
      <span>Institutional Grade</span>
    </div>

    <h1>Fix EMIR and SFTR reconciliation breaks faster.</h1>
    <p>RegParity turns reconciliation and valuation breaks into prioritised corrective actions — with a full audit trail and compliant outbound updates.</p>

    <div class="hero-actions">
      <a href="/download" class="btn primary">Request download link</a>
      <!-- Secondary button styled as ghost via base .btn class -->
      <a href="/contact" class="btn">Contact</a>
    </div>

    <div class="trust-strip">
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
          <line x1="9" y1="3" x2="9" y2="21"></line>
        </svg>
        <span>Windows Desktop</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        <span>Offline-First</span>
      </div>
      <div class="trust-item">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
          <polyline points="14 2 14 8 20 8"></polyline>
          <line x1="16" y1="13" x2="8" y2="13"></line>
          <line x1="16" y1="17" x2="8" y2="17"></line>
          <polyline points="10 9 9 9 8 9"></polyline>
        </svg>
        <span>Full Audit Trail</span>
      </div>
    </div>
  </div>

  <div class="panel hero-mockup">
    <!-- CSS-Only Mockup optimized for Dark Theme -->
    <div class="mockup-header">
      <div class="window-controls">
        <span></span><span></span><span></span>
      </div>
      <div class="window-title">RegParity Dashboard — Trade Reconciliation</div>
    </div>
    <div class="mockup-body">
      <div class="sidebar">
        <div class="sidebar-row active"></div>
        <div class="sidebar-row"></div>
        <div class="sidebar-row"></div>
        <div class="sidebar-row"></div>
      </div>
      <div class="main-view">
        <div class="stats-grid">
          <div class="stat-box">
            <span class="label">Breaks Found</span>
            <span class="value warning">124</span>
          </div>
          <div class="stat-box">
            <span class="label">Corrective Actions</span>
            <span class="value success">37</span>
          </div>
          <div class="stat-box">
            <span class="label">Pending Review</span>
            <span class="value neutral">12</span>
          </div>
        </div>
        <div class="data-table">
          <div class="table-header"></div>
          <div class="table-row">
            <span class="cell sm"></span><span class="cell lg"></span><span class="cell md"></span>
          </div>
          <div class="table-row">
            <span class="cell sm"></span><span class="cell lg"></span><span class="cell md"></span>
          </div>
          <div class="table-row">
            <span class="cell sm"></span><span class="cell lg"></span><span class="cell md"></span>
          </div>
          <div class="table-row">
            <span class="cell sm"></span><span class="cell lg"></span><span class="cell md"></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Layout Adjustments */
  .hero-actions {
    display: flex;
    gap: 16px;
    margin-bottom: 32px;
  }

  /* Trust Strip */
  .trust-strip {
    display: flex;
    gap: 24px;
    padding-top: 24px;
    border-top: 1px solid var(--line);
    color: var(--text-dim);
    font-size: 0.85rem;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  /* Mockup Styling */
  .hero-mockup {
    /* .panel class handles border/bg/shadow/radius */
    display: flex;
    flex-direction: column;
    min-height: 360px;
    aspect-ratio: 16/10;
  }

  .mockup-header {
    background: rgba(0,0,0,0.2);
    padding: 12px 16px;
    display: flex;
    align-items: center;
    border-bottom: 1px solid var(--line);
  }
  .window-controls {
    display: flex;
    gap: 6px;
    margin-right: 16px;
  }
  .window-controls span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--line); /* Muted controls */
  }
  .window-title {
    font-size: 0.75rem;
    color: var(--text-dim);
    font-family: monospace;
  }

  .mockup-body {
    flex: 1;
    display: flex;
  }

  .sidebar {
    width: 60px;
    border-right: 1px solid var(--line);
    padding: 16px 12px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    background: rgba(0,0,0,0.1);
  }
  .sidebar-row {
    height: 6px;
    border-radius: 3px;
    background: var(--line);
    opacity: 0.5;
  }
  .sidebar-row.active {
    background: var(--gold);
    opacity: 0.6;
    width: 60%;
  }

  .main-view {
    flex: 1;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .stats-grid {
    display: flex;
    gap: 16px;
  }
  .stat-box {
    flex: 1;
    background: rgba(255,255,255,0.03);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 12px;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .stat-box .label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--text-dim);
  }
  .stat-box .value {
    font-size: 1.4rem;
    font-weight: 700;
  }
  .value.warning { color: #FF6B6B; }
  .value.success { color: #51CF66; }
  .value.neutral { color: var(--text); }

  .data-table {
    flex: 1;
    border: 1px solid var(--line);
    border-radius: 8px;
    overflow: hidden;
  }
  .table-header {
    height: 24px;
    background: rgba(255,255,255,0.03);
    border-bottom: 1px solid var(--line);
  }
  .table-row {
    height: 28px;
    border-bottom: 1px solid var(--line);
    display: flex;
    align-items: center;
    padding: 0 12px;
    gap: 12px;
  }
  .table-row:last-child { border-bottom: none; }

  .cell { height: 6px; border-radius: 3px; background: var(--line); opacity: 0.3; }
  .cell.sm { width: 20%; }
  .cell.lg { width: 50%; }
  .cell.md { width: 30%; }

  @media (max-width: 900px) {
    .trust-strip { justify-content: center; }
  }
</style>
