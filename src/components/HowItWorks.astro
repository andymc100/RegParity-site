---
---
<section class="wrap section-spacing">
  <h2>How it works</h2>
  <div class="steps-grid">
    <div class="step">
      <div class="step-num">1</div>
      <h3>Load TR position files</h3>
      <p>Import position and reconciliation reports directly from your trade repository.</p>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <h3>Review breaks & corrections</h3>
      <p>Identify discrepancies and apply the exact corrective actions required.</p>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <h3>Export compliant updates</h3>
      <p>Generate validation-ready outbound files with a complete audit trail.</p>
    </div>
  </div>
</section>

<style>
  .section-spacing {
    padding: 80px 22px;
  }
  h2 {
    text-align: center;
    font-size: clamp(2rem, 3vw, 2.5rem);
    margin-bottom: 60px;
    color: var(--rpText);
  }
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }
  .step {
    position: relative;
    padding-right: 20px;
  }
  .step-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid var(--rpBlue);
    color: var(--rpBlue);
    font-weight: 700;
    margin-bottom: 24px;
    background: var(--rpBlue100);
  }
  .step h3 {
    margin: 0 0 12px;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--rpText);
  }
  .step p {
    color: var(--rpText);
    line-height: 1.6;
    margin: 0;
  }

  /* Connector Line */
  .step:not(:last-child)::after {
    content: '';
    position: absolute;
    top: 20px;
    left: 50px;
    right: -10px;
    height: 1px;
    background: var(--rpBorder);
  }

  @media (max-width: 768px) {
    .steps-grid { grid-template-columns: 1fr; gap: 40px; }
    .step:not(:last-child)::after {
      /* Vertical line for mobile? Or just hide it */
      display: none;
    }
  }
</style>
