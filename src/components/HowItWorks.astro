---
const steps = [
  {
    title: "Load TR position files",
    description: "Ingest massive position files and reconciliation reports directly from your TR/repositories.",
  },
  {
    title: "Review breaks & corrections",
    description: "RegParity surfaces the discrepancies and proposes the exact corrective actions needed.",
  },
  {
    title: "Export compliant updates",
    description: "Generate validation-ready outbound files with a complete, immutable audit trail attached.",
  },
];
---
<section class="how-it-works">
  <div class="container">
    <h2>How it works</h2>
    <div class="steps-container">
      {steps.map((step, index) => (
        <div class="step-item">
          <div class="step-header">
            <span class="step-number">{index + 1}</span>
            <h3>{step.title}</h3>
          </div>
          <p>{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .how-it-works {
    padding: 6rem 0;
    border-bottom: 1px solid var(--color-border);
  }
  h2 {
    font-size: 2rem;
    margin-bottom: 4rem;
    text-align: center;
  }
  .steps-container {
    display: grid;
    gap: 3rem;
  }
  @media (min-width: 768px) {
    .steps-container {
      grid-template-columns: repeat(3, 1fr);
      gap: 4rem;
    }
  }
  .step-item {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    position: relative;
  }

  /* Connecting line for desktop */
  @media (min-width: 768px) {
    .step-item:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 1.5rem;
      right: -2.5rem;
      width: 1rem;
      height: 2px;
      background: #e0e0e0;
    }
  }

  .step-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 0.5rem;
  }

  .step-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    background: transparent;
    color: var(--color-primary);
    border: 2px solid var(--color-primary);
    border-radius: 50%;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0;
  }

  p {
    font-size: 1rem;
    color: #666;
    margin: 0;
    line-height: 1.6;
    padding-left: 0.5rem; /* Slight align with header text visually */
  }
</style>
