---
import { Image } from 'astro:assets';
import actionImage from '../assets/screenshots/action-field-modifications.webp';
---

<section class="corrective-action">
  <div class="wrap container">
    <div class="grid-layout">
      <div class="content">
        <h2>Controlled, traceable corrective updates.</h2>
        <p>Review discrepancies, restore original values where required, and apply precise corrective updates — with traceability from break to outbound file.</p>
        <p class="anchor-line">Designed for EMIR and SFTR trade reporting teams.</p>
      </div>
      <div class="visual">
        <div class="screenshot-frame">
          <Image src={actionImage} alt="Corrective Action Interface" width={900} density={2} class="screenshot-img" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .corrective-action {
    background: #ffffff;
    padding: 100px 0;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .grid-layout {
    display: grid;
    grid-template-columns: 35fr 65fr;
    gap: 48px;
    align-items: center;
  }

  .content {
    padding-right: 16px;
  }

  h2 {
    font-size: clamp(2.2rem, 3.2vw, 2.8rem);
    line-height: 1.15;
    margin: 0 0 20px;
    color: var(--rpText);
    font-weight: 700;
  }

  p {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--rpTextMuted);
    margin: 0;
  }

  .anchor-line {
    font-size: 0.9rem;
    margin-top: 16px;
    color: var(--rpTextMuted);
    opacity: 0.8;
  }

  .screenshot-frame {
    background: white;
    padding: 0;
    border: 1px solid var(--rpBorder);
    border-radius: 12px;
    box-shadow: 0 14px 44px rgba(0,0,0,0.10);
    overflow: hidden;
  }

  .screenshot-img {
    display: block;
    width: 100%;
    height: auto;
    transform: scale(1.06);
    transform-origin: center;
  }

  @media (max-width: 900px) {
    .corrective-action {
      padding: 60px 0;
    }

    .grid-layout {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 40px;
    }

    .content {
      padding-right: 0;
      max-width: 600px;
      margin: 0 auto;
    }
  }
</style>
